import axios from 'axios'

const USE_MOCK = false

export async function getPermitStats(areaId){
  return null
}

  // Replace with a real endpoint when available:
  // const { data } = await axios.get(`https://…/permits?area=${areaId}`)
  // return data

